{
  "name": "nuxt-app",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare"
  },
  "dependencies": {
    "tailwindcss": "^4.0.14",
    "vue": "latest",
    "vue-router": "latest"
  },
  "overrides": {
    "vite": {
      "esbuild": "^0.25.0"
    }
  },
  "devDependencies": {
    "@nuxt/ui": "^3.0.1",
    "nuxt": "^3.16.1"
  }
}
